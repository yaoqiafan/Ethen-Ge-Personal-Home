# 前端修改任务清单

## 任务目标
将Vue前端从连接PF.AutoFramework后端改为连接OpenClaw网关。

## 核心修改（按优先级）

### 🔴 高优先级（必须完成）
1. **创建OpenClaw服务文件**
   - 文件：`src/services/openclawService.ts`
   - 内容：参考`前端修改技术说明.md`中的示例
   - 功能：提供系统状态、文件管理、WebSocket连接

2. **修改环境配置**
   - 文件：`.env.development`
   - 添加：
     ```
     VITE_OPENCLAW_API_URL=http://localhost:18789
     VITE_OPENCLAW_WS_URL=ws://localhost:18789/ws
     ```

3. **修改PFSystemStatus组件**
   - 文件：`src/components/framework/PFSystemStatus.vue`
   - 修改：导入`openclawService`代替`pfSystemService`
   - 修改API调用方法

4. **修改PFFileManager组件**
   - 文件：`src/components/framework/PFFileManager.vue`
   - 修改：导入`openclawService`代替`pfKnowledgeService`
   - 修改文件相关API调用

5. **修改PFMonitorConsole组件**
   - 文件：`src/components/framework/PFMonitorConsole.vue`
   - 修改：导入`useOpenClawMonitor`代替`usePFMonitor`
   - 修改WebSocket连接逻辑

### 🟡 中优先级（建议完成）
6. **更新TypeScript接口**
   - 确保`openclawService.ts`中的接口与组件兼容
   - 可以复用原有PF接口定义

7. **添加模拟数据**
   - 在`openclawService.ts`中提供模拟数据
   - 确保前端在没有真实OpenClaw API时也能显示

8. **修改Vite配置**
   - 文件：`vite.config.ts`
   - 添加OpenClaw代理（可选）

### 🟢 低优先级（可选）
9. **逐步移除PF依赖**
   - 最终可以删除`pfKnowledgeService.ts`
   - 统一使用`openclawService.ts`

10. **添加错误处理**
    - 增强OpenClaw连接的错误处理
    - 添加重试机制

## 具体代码修改示例

### 1. PFSystemStatus.vue 修改
```typescript
// 替换前
import { pfSystemService } from '@/services/pfKnowledgeService'

// 替换后
import { openclawService } from '@/services/openclawService'

// 修改API调用
async function fetchStatus() {
  try {
    // 原：status.value = await pfSystemService.getStatus()
    // 新：
    status.value = await openclawService.getSystemStatus()
  } catch (error) {
    console.error('获取系统状态失败:', error)
  }
}
```

### 2. PFFileManager.vue 修改
```typescript
// 替换前
import { pfKnowledgeService } from '@/services/pfKnowledgeService'

// 替换后
import { openclawService } from '@/services/openclawService'

// 修改文件加载
async function loadFiles() {
  try {
    // 原：const res = await pfKnowledgeService.getFiles(options)
    // 新：
    const res = await openclawService.getWorkspaceFiles()
    fileList.value = res.files ?? []
    directories.value = res.directories ?? []
  } catch (error) {
    console.error('加载文件失败:', error)
  }
}
```

### 3. PFMonitorConsole.vue 修改
```typescript
// 替换前
import { usePFMonitor } from '@/services/pfKnowledgeService'

// 替换后
import { useOpenClawMonitor } from '@/services/openclawService'

// 在setup中替换
const {
  isConnected,
  reconnectCount,
  logs,
  connect,
  disconnect,
  send,
  clearLogs: clearWsLogs,
} = useOpenClawMonitor()  // 替换 usePFMonitor()
```

## 测试验证步骤

1. **编译检查**
   ```bash
   npm run type-check
   npm run build
   ```

2. **运行测试**
   ```bash
   npm run dev
   ```

3. **功能验证**
   - 访问 http://localhost:5173
   - 检查PFSystemStatus组件是否显示数据
   - 检查PFFileManager组件是否能显示文件
   - 检查PFMonitorConsole组件是否能连接WebSocket

4. **错误检查**
   - 打开浏览器开发者工具
   - 检查Console是否有错误
   - 检查Network请求是否正常

## 回滚方案

如果修改失败，可以：
1. 恢复原版`pfKnowledgeService.ts`
2. 恢复组件中的导入语句
3. 暂时使用模拟适配器方案

## 时间估计
- 基础修改：2-3小时
- 完整测试：1小时
- 问题修复：1-2小时

## 交付标准
- ✅ 前端能正常编译运行
- ✅ 所有组件能显示数据
- ✅ WebSocket连接正常
- ✅ 没有控制台错误
- ✅ TypeScript类型检查通过

## 注意事项
1. 保持原有组件UI不变
2. 确保TypeScript类型安全
3. 添加适当的错误处理
4. 提供有意义的模拟数据
5. 保持代码可读性和可维护性